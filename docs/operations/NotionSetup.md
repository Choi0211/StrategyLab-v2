# Notion Setup

This phase provides dry-run Notion mapping and sync contracts only.

For future real use:

- provide token through `GAON_NOTION_TOKEN`
- set `GAON_NOTION_ENABLED=true`
- configure parent page and database IDs through environment variables

Notion is not the source of truth. Research Brain, Learning Memory, and Audit Log remain canonical.
